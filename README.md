Workflow
========

Some of the most command configuration, settings and process that will speed up your current workflow. Fire up your command prompt and execute the command.


## Bash Alias

    $ rm -f ~/.bash_aliases && curl -Sso ~/.bash_aliases https://raw.githubusercontent.com/groovey/Workflow/master/.bash_aliases

## Dir Colors

    $ rm -f ~/.dircolors && curl -Sso ~/.dircolors https://raw.githubusercontent.com/groovey/Workflow/master/.dircolors